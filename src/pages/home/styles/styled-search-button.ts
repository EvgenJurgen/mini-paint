import styled from 'styled-components';
import { HeaderButtonsItem } from '../../../core/styles/styled-header-buttons-item';

export const SearchButton = styled(HeaderButtonsItem)`
  @media (min-width: 768px) {
    font-size: 0;
  }
`;
