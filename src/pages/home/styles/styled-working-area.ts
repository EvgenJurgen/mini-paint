import styled from 'styled-components';
import { WorkingArea as WorkingAreaStyle } from '../../../core/styles/styled-working-area';

export const WorkingArea = styled(WorkingAreaStyle)`
  top: 0;
  transform: none;
`;
