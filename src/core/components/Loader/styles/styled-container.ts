import styled from 'styled-components';

export const Container = styled.div`
  width: 10rem;
  height: 10rem;
  position: absolute;
  left: 0;
  right: 0;
  margin: 0 auto;
  top: 50%;
  transform: translate(0, -50%);
`;
